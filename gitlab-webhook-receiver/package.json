{
  "name": "docker-event-watcher",
  "version": "1.0.0",
  "description": "Component that listens for Docker events for new containers",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon --watch '**/*.ts' --exec 'tsc index.ts && node index.js'",
    "build": "tsc"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.15.2",
    "express": "^4.14.0",
    "redis": "^2.6.3"
  },
  "devDependencies": {
    "nodemon": "^1.11.0",
    "typescript": "^2.1.4"
  }
}
